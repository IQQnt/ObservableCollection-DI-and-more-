<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Controles -->
        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="🔄 Cargar Datos" 
                        Command="{Binding LoadDataCommand}"
                        Margin="0,0,5,0"/>
                <Button Content="🗑️ Limpiar" 
                        Command="{Binding ClearItemsCommand}"
                        Foreground="Red"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" 
                         Width="200" Padding="5" Margin="0,0,5,0"/>
                <Button Content="➕ Añadir" 
                        Command="{Binding AddItemCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- Lista -->
        <Border Grid.Row="1" Margin="0,10,0,0" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <!-- Overlay de loading -->
                <Border Background="#80000000" 
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Cargando..." 
                               Foreground="White" 
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Center"
                               FontSize="16"/>
                </Border>

                <!-- Lista de items -->
                <ListView ItemsSource="{Binding Items}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Created, StringFormat='Creado: {0:HH:mm:ss}'}" 
                                           Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="2" 
                   Text="{Binding Items.Count, StringFormat='Total items: {0}'}"
                   Margin="0,10,0,0"/>
    </Grid>

</Window>
