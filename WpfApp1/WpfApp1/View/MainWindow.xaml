<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Grid Margin="10">
        <StackPanel>
            <!--<Button Content="➕ Añadir Item" 
                    Height="35" FontSize="14" Margin="0,0,0,10"/>-->

            <TextBlock Text="📦 Items Compartidos:" 
                       FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>

            <ListView ItemsSource="{Binding Items}" Height="300">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="5">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="14"/>
                                <TextBlock Text="{Binding Created, StringFormat='🕐 {0:dd/MM/yyyy HH:mm:ss}'}" 
                                           Foreground="Gray" FontSize="11"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Text="{Binding Items.Count, StringFormat='Total: {0} items'}" 
                       HorizontalAlignment="Right" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
    
</Window>
